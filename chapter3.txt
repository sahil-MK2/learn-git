default branch is master branch
empty folder always egnore

git init --initial-branch main

git status -v (for mare detatils )
git status -s (for less details )

to remove from stash area

git rm --cached file.txt file2.txt

all about commmit 
	commit object 
		usser email 
		time
		commit msg
		snapshot of file in stagging area 
when ever commit encrypt with sha-256

git commit -am "commit msg" for already added file

git commit --amed (to add in last commit) --no-edit

git commit -s -m "signoff msg "(for signoff at end of task)

git commit --allow empty -m "commit msg"

